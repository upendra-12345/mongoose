const mongoose= require("mongoose");
const blogSchema = new mongoose.Schema({
    title:{
        type:String,
        required:true,
    },
    content:{
        type:String,
        minLenght:20,
    },
    created_at:{
        type:Date,
        required:true,
    },
});
const blogModel = mongoose.model("Blog",studentSchema);
module.exports=blogModel;